import { NewsCheckPipePipe } from './news-check-pipe.pipe';

describe('NewsCheckPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NewsCheckPipePipe();
    expect(pipe).toBeTruthy();
  });
});
